import {
    StyleSheet,
    TextInput,
    View,
    Text,
    Image,
    StatusBar,
    TouchableOpacity,
    KeyboardAvoidingView,
    TouchableWithoutFeedback,
    Keyboard,
  } from "react-native";

  const Home = ({ navigation }) => {
      return (
          <Text>
              Hi
          </Text>
      );
  };